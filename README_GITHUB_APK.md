# GitHub APK Build

This package is arranged so that the Android project is at the GitHub repository root.

After extracting, upload all extracted files and folders to the root of a GitHub repository. The GitHub Actions workflow is located at `.github/workflows/build-apk.yml`.

Then open the repository's Actions tab and run `Build debug APK`.
